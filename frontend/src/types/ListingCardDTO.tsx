export interface ListingCardDTO {
  id: number;
  name: string;
  price: number;
  basePrice: number;
  favoritedCount: number;
  gameStoreName: string;
  gameStoreImagePath: string;
  gameImagePath: string;
  region: string;
  cashback: number;
  discountPercent: number;
}
